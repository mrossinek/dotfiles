text/html; qutebrowser '%s' &; test=test -n "$DISPLAY"; needsterminal;
text/html; w3m -I %{charset} -T text/html; copiousoutput;
image/*; ~/.config/neomutt/image_preview.sh %s; needsterminal;
application/pdf; zathura %s; needsterminal;
